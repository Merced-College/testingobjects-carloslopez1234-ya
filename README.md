1.Before the toString, it showed a memory address.  

2. The 3 defaults were the string, int and string and their fields were null and 0.

3. After adding the default constructor, it had a few errors in the main. Adding the constructors affects the defsaults. 

4. @override prints the data variables instead of the memory address. It is useful to add more information in the code. 

5. It is standerdized and it helps it be secured and if you want to add more stuff if its in private. 